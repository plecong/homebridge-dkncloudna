{
  "pluginAlias": "dkncloudna",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "headerDisplay": "Enter your login information for the DKN Cloud NA app",
  "schema": {
    "type": "object",
    "properties": {
      "email": {
        "title": "Email Address",
        "description": "Email address to login to DKN Cloud NA app",
        "type": "string",
        "required": false,
        "format": "email"
      },
      "password": {
        "title": "Password",
        "description": "Password to login to DKN Cloud NA app",
        "type": "string",
        "required": false
      },
      "token": {
        "title": "Access Token",
        "description": "Token used to access DKN Cloud NA API",
        "type": "string",
        "required": false
      },
      "refreshToken": {
        "title": "Refresh Token",
        "description": "Token used to refresh the Access Token when expired",
        "type": "string",
        "required": false
      },
      "enableExterior": {
        "title": "Enable Exterior Temperature Sensor",
        "description": "Enable the Exterior Temperature Sensor (restart required)",
        "type": "boolean",
        "required": false
      },
      "enableFan": {
        "title": "Enable Fan Control (Experimental)",
        "description": "Enable the Fan V2 service to control fan mode, rotation speed, and swing mode (restart required)",
        "type": "boolean",
        "required": false
      }
    }
  }
}
